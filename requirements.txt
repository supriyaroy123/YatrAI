# YatrAI - Dependencies
# Core ML
lightgbm>=4.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# API & Web
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0
pydantic>=2.5.0
httpx>=0.24.0          # async HTTP client for parallel Overpass API calls

# Database
aiosqlite>=0.19.0

# Firebase Admin SDK (Firestore POI cache)
firebase-admin>=6.2.0

# POI Search: Local embeddings + vector search
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.2.0   # all-MiniLM-L6-v2, runs on CPU, ~90 MB
faiss-cpu>=1.7.4               # in-process vector index, no GPU needed

